import {
  destroyUser, createNewUser, authUser, registerUserInApp, updateUser
} from './_user'
import toggleSidebar from './_ui'

export {
  destroyUser, toggleSidebar, createNewUser, authUser, registerUserInApp,
  updateUser
}
